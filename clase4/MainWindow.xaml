<Window x:Class="clase4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:clase4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition>
               
            </ColumnDefinition>
            <ColumnDefinition>
                
            </ColumnDefinition>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <Label Content="Paises">
                
            </Label>
            <CheckBox Content="Seleccionar Todos" Name="TodosCheck" IsThreeState="True">
            </CheckBox>
            <CheckBox Content="Palestina" Name="palestinaCheck" IsThreeState="True">
            </CheckBox>

            
        </StackPanel>
        
        <StackPanel Grid.Column="1">
            <ComboBox Name="ComboBoxCiudades">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding nombre}"> </TextBlock>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
                
            </ComboBox>
        </StackPanel>
    </Grid>
</Window>
